import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        /*
        for(int i = 0; i < 10; i++) { // 2 < 10 (true)
            if(i % 2 == 0) {
               continue;
            }
            System.out.println(i); // 1
        }


        // break: khi gặp break dừng vòng lặp gan nhất
        // continue: Khi gặp continue, bỏ qua lần lặp hiện tại (các đoạn code bên dưới và chuyển tới lần lặp tiếp theo

         int i = 10;
            do {
                System.out.println(i);
                i--;
            } while (i > 0);

        // do while dù đúng hay sai thì cũng thực thi ít nhất 1 lần
        // Cho người dùng nhập số và dừng lại khi số đó bẳng 0 (dùng do while);
        Scanner input = new Scanner(System.in);
        int value;
        do {
            System.out.println("Nhập số đi bạn ơi: ");
            value = input.nextInt();
        } while (value != 0);
       */

        /*
        while (<điều kiện dừng>) {
            <câu lệnh thực thi>
        }
        nếu <điều kiện dừng> trả về false thì sẽ lập tức dừng vòng lặp
        int i = 0;
        while (i < 10) {
            System.out.println(i);
            i++;
        }
         */


    }


}